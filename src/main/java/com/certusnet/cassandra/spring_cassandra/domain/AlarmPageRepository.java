package com.certusnet.cassandra.spring_cassandra.domain;

import java.util.List;

import org.springframework.data.domain.Pageable;
import org.springframework.data.repository.Repository;

//public interface AlarmPageRepository extends Repository<AlarmSn, String> {
//
//	List<AlarmSn> findBySnOrBeginDate(String sn, Integer beginDate, Pageable pageable);
//}
